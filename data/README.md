# Data directory

- `hardware/`: declared hardware/effective-rate profiles;
- `workloads/`: general and sparse workload profiles;
- `observed/`: direct measurements with provenance;
- `analytical/`: deterministic outputs of the mathematical model;
- `schemas/`: machine-readable input contracts.

Never place credentials, prompts, generated text, private source code or customer data in committed route traces.
